# Write directly to containerd's persistent writable snapshot.
# gVisor's default root:self overlay is ephemeral across task recreation.
[runsc_config]
  overlay2 = "none"
  # File APIs modify the snapshot outside the sentry; revalidate cached entries.
  file-access = "shared"
